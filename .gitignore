# Dependencies
node_modules/

# Build outputs
dist/
build/
out/
.next/
*.tsbuildinfo
next-env.d.ts

# Testing
coverage/
test-results/
playwright-report/
.playwright/

# צילומי מסך לבדיקת עיצוב (`npm run visual`) — נוצרים מחדש בכל הרצה
.visual/

# Environment & secrets
.env
.env.*
!.env.example

# Editor / OS
.vscode/
.idea/
Thumbs.db
.DS_Store

# Logs & temp
*.log
tmp/

# Prisma — נוצר מחדש ב-`prisma generate`, לא נשמר בגיט
/src/generated/prisma

# בסיס הנתונים המקומי (PostgreSQL שרץ מ-node_modules)
.localdb/

# מיומנויות סוכן ש-`prisma init` מייצר. מכילות סימלינקים ו-~500KB תיעוד
# שאפשר לשחזר בכל רגע; אין סיבה לגרור אותן דרך הריפו.
.agents/
.claude/
.windsurf/
skills-lock.json

# מדיה שנשמרת מקומית בפיתוח (בפרודקשן הכול ב-Cloudflare R2)
.localmedia/

# גיבויי בסיס הנתונים בפיתוח (בפרודקשן ב-bucket השני ב-R2)
.backups/
