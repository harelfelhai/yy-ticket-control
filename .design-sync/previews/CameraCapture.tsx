import { CameraCapture } from "yy-ticket-control";

/**
 * צילום ישיר מהמצלמה האחורית של הטלפון, כשכבה מלאה מעל המסך. הרכיב מבקש
 * גישה למצלמה בעת ההרכבה; בסביבה ללא מצלמה הוא מציג את מצב השגיאה שלו,
 * וזה בדיוק מה שמשתמש שדחה את ההרשאה רואה.
 *
 * מרונדר תמיד פתוח: זהו מצבו היחיד — רכיב סגור אינו מרנדר דבר.
 */
export function Open() {
  return <CameraCapture onCaptured={() => {}} onClose={() => {}} />;
}
