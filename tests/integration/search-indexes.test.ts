import { describe, expect, it } from "vitest";
import { db } from "@/lib/db";

/**
 * אינדקסי החיפוש החופשי — קיימים, **ומחלצים שלשות מעברית**.
 *
 * שתי בדיקות שנולדו משתי תקלות נפרדות, ושתיהן חיו חודשים בלי שאף בדיקה
 * נכשלה. זו הסיבה שהן כאן: אינדקס הוא הדבר היחיד במערכת שיכול להיעלם בלי
 * לשנות ולו תוצאה אחת — רק את הזמן שלוקח להגיע אליה.
 *
 * **תקלה 1 — האינדקסים נמחקו בשוגג.** ארבעתם נוצרו ב-SQL גולמי ולא הוצהרו
 * ב-`schema.prisma`, ולכן `20260724054950_add_rate_limit` — מיגרציה שכל
 * תוכנה היה הוספת טבלה — סיווגה אותם כעודפים ומחקה אותם. הם מוצהרים היום,
 * וההצהרה היא ההגנה האמיתית; הבדיקה הראשונה כאן היא רשת הביטחון השנייה.
 *
 * **תקלה 2 — ואת זו ההצהרה לא הייתה תופסת.** `pg_trgm` מסווג תווים לפי
 * ה-`LC_CTYPE` של **בסיס הנתונים**. תחת `C`, כל בית מעל 0x80 אינו נחשב
 * אות — כלומר `show_trgm('נזילה')` מחזיר `{}`, האינדקס ריק מעברית, וכל
 * שאילתה עברית מחזירה את **כל** השורות כמועמדות ואז מסננת אותן ידנית.
 * זה גרוע מסריקה רגילה: אותה עבודה, בתוספת האינדקס.
 *
 * במילים אחרות, האינדקסים לא רק נמחקו — **גם בשני הימים שבהם הם היו
 * קיימים הם לא עשו דבר עבור השפה שהמערכת כתובה בה.** הבדיקה השנייה כאן
 * היא היחידה שיכולה לתפוס את זה, ואין לה תחליף בהצהרה או בסקירת קוד:
 * ‏`COLLATE` ברמת ביטוי אינו מתקן אותה (נבדק), מפני שהוא משפיע על סדר
 * השוואה ולא על סיווג תווים.
 *
 * נמדד על 20,000 שורות: עם locale תקין השאילתה לוקחת 1.6ms ומחזירה בדיוק
 * את השורות התואמות; בלי אינדקס — 24.8ms.
 */

/** ארבעת האינדקסים כפי ש-Prisma מייצר אותם מההצהרות ב-`schema.prisma`. */
const TRIGRAM_INDEXES = [
  "Ticket_description_idx",
  "Message_text_idx",
  "MediaFile_transcription_idx",
  "MediaFile_extractedText_idx",
];

describe("אינדקסי החיפוש החופשי", () => {
  it("ארבעת האינדקסים קיימים בבסיס הנתונים", async () => {
    const rows = await db.$queryRaw<{ indexname: string }[]>`
      SELECT indexname FROM pg_indexes WHERE schemaname = current_schema()
    `;
    const present = new Set(rows.map((row) => row.indexname));
    const missing = TRIGRAM_INDEXES.filter((name) => !present.has(name));

    expect(
      missing,
      `אינדקס חיפוש חסר — בדוק אם מיגרציה הפילה אותו (קרה ב-add_rate_limit):\n${missing.join("\n")}`,
    ).toEqual([]);
  });

  it("‏pg_trgm מחלץ שלשות מעברית — אחרת האינדקס ריק ממה שמחפשים בו", async () => {
    const [{ trigrams }] = await db.$queryRaw<{ trigrams: string[] }[]>`
      SELECT show_trgm('נזילה') AS trigrams
    `;

    expect(
      trigrams.length,
      "‏show_trgm על מילה עברית החזיר קבוצה ריקה — ה-LC_CTYPE של בסיס הנתונים " +
        "אינו מודע ל-UTF-8 (כנראה 'C'). האינדקסים קיימים אך אינם מכילים עברית, " +
        "וכל חיפוש עברי סורק את כל הטבלה. התיקון דורש יצירה מחדש של בסיס " +
        "הנתונים עם locale תקין — ALTER אינו מספיק.",
    ).toBeGreaterThan(0);
  });

  it("האינדקס באמת משרת `ILIKE` בעברית ואינו רק קיים", async () => {
    /**
     * הבדיקה שמחברת בין השתיים: קיום פלוס חילוץ אינם מספיקים אם המתכנן
     * אינו יכול להשתמש באינדקס לשאילתה שהמערכת באמת מריצה.
     *
     * ‏`enable_seqscan = off` נדרש מפני שטבלת הבדיקות קטנה, ובטבלה קטנה
     * המתכנן בוחר סריקה **בצדק** — 11 עמודים זולים מאינדקס. הכפייה שואלת
     * את השאלה הנכונה: "האם האינדקס *יכול* לשרת את זה", ולא "האם הוא
     * נבחר בגודל הנוכחי".
     *
     * **בתוך `$transaction`, וזה לא קישוט.** ‏`SET LOCAL` תקף לטרנזקציה
     * בלבד, ו-Prisma מחלק שאילתות בין חיבורים ב-pool — שתי קריאות נפרדות
     * יכולות לנחות על חיבורים שונים, ואז ההגדרה פשוט אינה חלה על ה-`EXPLAIN`.
     * הניסוח הראשון כאן עשה בדיוק את זה ונכשל בשקט על סריקה.
     */
    const text = await db.$transaction(async (tx) => {
      await tx.$executeRawUnsafe("SET LOCAL enable_seqscan = off");
      const plan = await tx.$queryRawUnsafe<{ "QUERY PLAN": string }[]>(
        `EXPLAIN SELECT id FROM "Ticket" WHERE description ILIKE '%נזילה%'`,
      );
      return plan.map((row) => row["QUERY PLAN"]).join("\n");
    });

    expect(text, `המתכנן אינו משתמש באינדקס הטריגרם:\n${text}`).toContain(
      "Ticket_description_idx",
    );
  });
});
