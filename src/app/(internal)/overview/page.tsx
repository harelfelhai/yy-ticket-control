import { redirect } from "next/navigation";

/**
 * סקירת האתרים עברה לראש מסך הניהול (`/admin`) — וזה מה שנשאר מהמסך שהיה.
 *
 * **למה הפניה ולא מחיקה.** הכתובת יצאה החוצה: היא הייתה קישור בסרגל הניווט
 * במשך כל חיי המערכת, ולכן היא יושבת בסימניות ובהיסטוריה של הדפדפן אצל כל
 * בעלים ומנהל. 404 היה נקרא כשבירה, לא כמעבר.
 *
 * ההרשאה אינה נבדקת כאן במתכוון — היא נבדקת ביעד. שער `/admin` מרחיק מנהל
 * עבודה ללוח בדיוק כפי שהמסך הזה עשה, ובדיקה כפולה כאן הייתה מקור אמת שני
 * לאותו כלל.
 */
export default function OverviewRedirect() {
  redirect("/admin");
}
