/**
 * רמות הכותרת — מקור אמת אחד.
 *
 * **החלוקה היא לפי תפקיד ולא לפי רמת ה-HTML.** ‏`<h1>` אינו בהכרח 24px:
 * התקן מבחין בין כותרת **מזהה** ("איפה אני" — איזו פנייה, איזו תגית) לבין
 * כותרת **מתארת**, ששמה מסך שהמשתמש ניווט אליו בכוונה. "חיפוש" אינו נושא
 * מידע שהמשתמש לא ידע לפני הלחיצה, ובגודל מלא הוא גוזל גובה מסך בלי להחזיר
 * דבר. לכן `<h1>חיפוש</h1>` הוא 20px — הרמה הסמנטית והגודל הוויזואלי הם
 * שתי החלטות נפרדות.
 *
 * הקבועים קיימים מפני שהמחרוזות **כבר סטו**: 15 כותרות סקציה נכתבו ב-14px,
 * כלומר **קטנות מהטקסט שהן מכותרות**. הסטייה לא נראתה מקובץ בודד, רק
 * מהשוואה בין מסכים.
 *
 * `tests/unit/typography.test.ts` נכשל אם `<h1>` או `<h2>` נושאים מחלקת
 * גודל גולמית, כדי שהסטייה לא תוכל לחזור בשקט.
 *
 * מקור: `docs/DESIGN.md` § Typography, "מזהה מול תיאורי".
 */

/** מזהה — "איפה אני": שם הפנייה, שם התגית, הנמען בפורטל. */
export const TITLE_IDENTIFYING = "text-2xl font-bold";

/** מתאר — שם מסך, כותרת קיבוץ, כותרת פאנל או כרטיס. */
export const TITLE_DESCRIPTIVE = "text-xl font-semibold";

/**
 * ערך מספרי באריח מדד (מסך הסקירה).
 *
 * **אותה רמה של `TITLE_IDENTIFYING`, ובכוונה קבוע נפרד.** הסקאלה סגורה בשש
 * רמות (DESIGN.md § חוקים), וזהו **תפקיד שני על רמה קיימת** — בדיוק כמו
 * `body` ו-`bodyStrong` שחולקים 16px בטבלה.
 *
 * ייבוא של `TITLE_IDENTIFYING` במקומו היה קובע שמספר הוא "כותרת מזהה",
 * ושינוי עתידי בגודל כותרת הדף היה מזיז את המדדים איתו — בשקט.
 */
export const METRIC_VALUE = "text-2xl font-bold";

/**
 * רוחבי תוכן — מקור אמת אחד.
 *
 * **אין יותר תקרה על `<main>`.** עד סבב הרוחב המלא היה קאפ אחד של 1024px על
 * כל המסכים הפנימיים, ובתוכו קאפ שני של 768px בכל עמוד — כלומר מסך של 1920px
 * הראה עמודה ממורכזת ברוחב שליש, ושני שלישים אפור. במערכת שכל תפקידה הוא
 * לסרוק פניות, זה גלילה במקום מידע.
 *
 * מה שהחליף אותו: **הרוחב נבחר לפי מה שעושים במסך, לא לפי המכשיר.** מסך
 * שסורקים בו (לוח, טבלאות ניהול) לוקח את כל הרוחב; מסך שקוראים בו (שרשור,
 * צ׳אט, טופס) נשאר בעמודה קריאה; פאנל יחיד נשאר פאנל.
 *
 * לכן כל מסך בוחר את הרוחב שמתאים לתפקידו, מתוך הרשימה הזו בלבד.
 * מקור: `docs/DESIGN.md` § Layout.
 */

/**
 * טקסט שנקרא ברצף: שרשור פנייה, צ׳אט תגית, טופס פנייה חדשה.
 *
 * ‏768px הוא רוחב שבו שורת טקסט נשארת קריאה (לא יותר מדי תווים לשורה) ותג
 * שנצמד לקצה עדיין נקרא כשייך לשורה שלו.
 *
 * **הצטמצם לתפקידו האמיתי.** עד סבב הרוחב המלא הוא היה ברירת המחדל של 14
 * מסכים, כולל טבלאות ניהול ורשימות רשומות — מסכים שאין בהם שורת טקסט ארוכה
 * אחת, ושרק שילמו על הקאפ בגלילה מיותרת. עכשיו הוא נבחר במפורש, ורק כשיש
 * מה לקרוא.
 */
export const CONTENT_WIDTH = "mx-auto w-full max-w-3xl";

/**
 * מסכי ניהול ורשימות — כל מה שסורקים, לא קוראים.
 *
 * ‏`w-full` בלי תקרה ובלי מירכוז: הלוח, הטבלאות ומסכי הניהול נפרשים על כל
 * המסך. זו היפוך של ההחלטה הקודמת (תקרת 1024px), והנימוק שלה עדיין תקף
 * בנקודה אחת — "ברוחב 1400px תג הסטטוס נותר מאות פיקסלים מהכותרת שהוא
 * מתאר". התשובה אינה לצמצם את העמוד אלא לתת את התקרה **לרכיב**: כרטיסי
 * הלוח יושבים בגריד שכל עמודה בו חסומה, ועמודת התיאור בטבלה חסומה בפני
 * עצמה. כך רוחב המסך קונה עוד כרטיסים בשורה — ולא כרטיס מתוח אחד.
 */
export const FULL_WIDTH = "w-full";

/**
 * הריפוד האופקי של עמוד, ושל כל רצועה דביקה שנמתחת לקצותיו.
 *
 * קבוע ולא מחרוזת בכל עמוד: הרצועות הדביקות (כותרות קיבוץ בלוח, שורת
 * הפעולות בפנייה) נמתחות לקצה ב-`-mx-*` שחייב להיות **בדיוק** הריפוד של
 * העמוד. כשהריפוד היה `p-4` כתוב ידנית ב-14 עמודים, כל שינוי בו היה מחייב
 * לזכור שלושה `-mx-4` מפוזרים — ומי ששוכח מקבל או פס ריק בצדדים שהתוכן
 * זולג דרכו, או גלישה אופקית שנכשלת רק בבדיקת המובייל.
 */
export const PAGE_X = "px-3";
export const PAGE_BLEED = "-mx-3";

/**
 * גובה סרגל הניווט, והיסט העגינה של כל רצועה דביקה שמתחתיו.
 *
 * **התלות הזו הייתה קיימת ולא הייתה כתובה.** ‏`h-14` בסרגל ו-`top-14` בשתי
 * רצועות דביקות היו שלוש מחרוזות נפרדות שחייבות להסכים; כשהסרגל ירד ל-44px
 * בסבב הצפיפות, שתי הרצועות היו נשארות תלויות באוויר 12px מתחת למקומן —
 * פער שנראה כמו באג רינדור ולא כמו מספר שמישהו שכח לעדכן.
 */
export const HEADER_HEIGHT = "h-11";
export const STICKY_UNDER_HEADER = "top-11";

/**
 * פאנל יחיד ממורכז: התחברות, קישור שפג, סיכום פעולה. תפקיד שלישי שהתגלה
 * בסבב הביקורת הראשון — שלושת המסכים האלה המציאו כל אחד ערך משלו
 * (`max-w-sm`, `max-w-sm`, `max-w-lg`), כי לתקן לא היה ערך לתת להם.
 * ‏448px: רחב מספיק לשני שדות וכפתור, צר מספיק להיקרא כפאנל ולא כעמוד.
 */
export const PANEL_WIDTH = "mx-auto w-full max-w-md";

/**
 * טופס הזנה שיושב **לצד** התוכן שהוא מוסיף אליו, ולא מעליו.
 *
 * נולד מהמקרה של "משתמש חדש": שישה שדות בעמודה אחת ברוחב העמוד דחקו את
 * רשימת המשתמשים מתחת לקו הקיפול בטלפון — 574px של טופס לפני שהמשתמש
 * הראשון מופיע. הטופס לא היה גדול מדי בפני עצמו; הוא פשוט ירש את רוחב
 * העמוד מפני שלא היה לו רוחב משלו.
 *
 * ‏320px: מספיק לשדה עם תווית, צר מספיק כדי שהרשימה תישאר גלויה לצדו
 * במסך רחב. במסך צר הוא נערם מעליה — שם אין ברירה, ולכן הוא גם קצר.
 */
export const FORM_PANEL_WIDTH = "w-full lg:max-w-80";

/**
 * קישור בתוך טקסט, ופעולה שקטה בתוך שורה.
 *
 * **קו תחתון ולא צבע, וזו תוצאה ישירה של המעבר לגרפיט.** כשצבע הפעולה היה
 * כחול, `text-brand` היה סימן מספיק. עכשיו הוא כמעט-שחור — ניגודיות 1.26
 * מול טקסט גוף, כלומר אותו צבע — ו-12 קישורים במערכת היו הופכים לטקסט רגיל
 * שאי אפשר לדעת שלוחצים עליו. התקן ממילא אוסר "צבע כנשא מידע יחיד"
 * (§ Do's and Don'ts); כאן האיסור נעשה מחייב גם לקישורים ולא רק לסטטוס.
 *
 * ‏`decoration-border` ולא קו מלא: קו בעובי ובצבע הטקסט מרעיש שורת מטא-דאטה
 * שיש בה שני קישורים. הקו מתחזק במעבר עכבר, וזה ההבדל בין "אפשר ללחוץ"
 * ל"אני עומד ללחוץ".
 */
export const LINK = "underline decoration-border underline-offset-4 hover:decoration-fg";

/**
 * גוף דיאלוג שתוכנו עלול לגלוש — נמענים, תגיות, ומטא-דאטה בפנייה אחת.
 *
 * ‏`Dialog` עצמו אינו מגביל גובה, ובצדק: חלון הצילום מחזיק תמונת וידאו
 * בגודל קבוע. אבל פאנל שגדל עם הנתונים — שמונה נמענים בפנייה עמוסה —
 * גולש מהמסך, וב-`fixed` פירוש הדבר שהתוכן התחתון **אינו נגיש כלל**: אין
 * גלילה לדף שמאחורי כיסוי, וכפתור "הסר" של הנמען האחרון יוצא מהישג יד.
 *
 * ‏`70vh` ולא ערך בפיקסלים: הקטע שנחתך תלוי בגובה המסך, לא ברוחבו, וסולם
 * הגבהים של Tailwind מודד ב-rem. זהו ערך יחיד ומוגדר, ולכן הוא יושב כאן
 * ולא נכתב בגוף רכיב (DESIGN.md § Dialog).
 */
export const DIALOG_SCROLL_BODY = "max-h-[70vh] overflow-y-auto";

/**
 * ריתמוס רשימה אנכית — שני התפקידים שהתקן מגדיר (DESIGN.md § ריתמוס).
 *
 * **למה קבועים ולא מחלקות ישירות.** פער 22 יישר את הריתמוס ידנית, ופער 32
 * גילה שהוא זלג לחמש רשימות שנכתבו אחריו — שתיים מהן מסכי ניהול שנכתבו
 * באותו סבב וחלוקים ביניהם. מחרוזת שחוזרת בעשרים מקומות מתפצלת; קבוע לא.
 *
 * הבחירה בין השניים היא **הצהרה על התפקיד**, ולכן היא בדיוק מה שאפשר
 * לסקור: פריטים נפרדים או שורות של אותו פריט.
 */

/** פריטים נפרדים ברשימה — כרטיסים, בועות שרשור, שורות ניהול */
export const CARD_LIST = "flex flex-col gap-2";

/** שורות **בתוך** פריט אחד — דירות בתוך בניין, קבצים בתוך בועה */
export const ROW_LIST = "flex flex-col gap-1";

/**
 * שם רשומה בשורת רשימת ניהול — אתר, תגית, דירה, תחום, איש מקצוע, משתמש.
 *
 * **פער 35: אותו תפקיד בדיוק נכתב בשלוש רמות.** ‏20px/600 לבניין,
 * ‏16px/600 לאתר ולתגית, ו-16px/500 לארבע הנותרות — בשש רשימות שמשתמשות
 * באותם שני פקדים משותפים (`InlineRename`, `DeleteButton`). ובנוסף
 * ‏16px/500 **אינו זוג בסקאלה**: § Typography מזווג 16px ל-400 או ל-600.
 *
 * ‏600 ולא 500, כי זה הזוג החוקי הקרוב. הגודל נשאר גודל הבסיס — שם רשומה
 * אינו כותרת, הוא התוכן של השורה.
 *
 * **הבניין נשאר `TITLE_DESCRIPTIVE` ובכוונה:** הוא כותרת של רשימת דירות
 * מקוננת, לא שורה. השוואתו לשורות הייתה מוחקת את ההיררכיה שבין בניין
 * לדירותיו — כלומר מתקנת עקביות ושוברת מבנה.
 */
export const RECORD_NAME = "font-semibold";
