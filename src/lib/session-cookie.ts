/**
 * שם עוגיית ההתחברות, במודול נפרד ובלי שום תלות.
 *
 * ‏`proxy.ts` ו-`session.ts` שניהם צריכים אותו, אבל `session.ts` מייבא את
 * `next/headers` ואת מפתח ההצפנה — דברים שאין להם מקום בשכבת ה-proxy.
 * מודול קטן ומשותף שומר על מקור אמת אחד בלי לגרור את כל השרשרת.
 */
export const SESSION_COOKIE_NAME = "yy_session";
