/**
 * נוסחי האפיון — מקור אמת לבדיקות ההתאמה.
 *
 * **הקובץ הזה קיים כדי לא לייבא מ-`src/lib/he.ts`.** בדיקה שמייבאת את
 * המחרוזת מהקוד מוכיחה רק שהקוד עקבי עם עצמו: אם `he.ts` נכתב בטעות עם
 * נוסח אחר מזה שבאפיון, ההשוואה תעבור והפער יישאר סמוי. לכן כל מחרוזת כאן
 * הועתקה **ידנית מטקסט האפיון**, ולצידה מספר השורה במסמך שממנה נלקחה:
 * `docs/specs/ticket-control-pre-plan.md`.
 *
 * מקום שבו האפיון משתמש בסוגריים מרובעים (`[שם]`, `[שמות]`, `[שם הפותח]`)
 * מיוצג כאן כפונקציה או כתבנית, ולא כמחרוזת קבועה — כי הערך משתנה בזמן ריצה
 * והחלק הקבוע הוא מה שנבדק.
 */

/** קבוצות הלוח — מסך 1, שורות 185–189 */
export const BOARD = {
  /** שורה 185. שים לב: §2.2 שלב 7 (שורה 59) כותב "דורש ממני" — סתירה פנימית GAP-01 */
  actionRequired: "דורש ממך",
  withRecipients: "אצל הנמענים",
  archive: "ארכיון",
  newTicket: "+ פנייה חדשה",
} as const;

/** ארבעת נוסחי טקסט-הסיבה המחייבים — מסך 1, שורות 199–202 */
export const REASON_EXAMPLES = {
  // ‏"יוסי שאל שאלה" עד 0.4. הכפתור הוסר, והנוסח ירש את מקומו ואת צורתו:
  // מבחינת המנהל זו אותה פעולה — מישהו בשטח פנה אליו וממתין.
  message: (name: string) => `${name} כתב הודעה`,
  partial: (done: number, total: number) => `${done} מתוך ${total} סיימו`,
  stale: (days: number) => `ללא תנועה ${days} ימים`,
  handler: (name: string) => `${name} מטפל`,
  /** מסך 1, טיוטה ממייל (עדכון 1.3, EM-S1-02) — הראשונה שמתקיימת */
  emailDraftConflicts: (fields: number) =>
    fields === 1 ? "טיוטה ממייל · סתירה ב-1 שדה" : `טיוטה ממייל · סתירה ב-${fields} שדות`,
  emailDraftMissing: (details: number) =>
    details === 1 ? "טיוטה ממייל · חסר 1 פרט" : `טיוטה ממייל · חסרים ${details} פרטים`,
  emailDraftReady: "טיוטה ממייל · מוכנה לשליחה",
} as const;

/** סטטוס ברמת השיוך — §3.4, שורות 141–146 */
export const ASSIGNMENT_STATUS = {
  /** "שויך" ולא "נשלח" מ-1.1 — האפיון תוקן, ראו §3.4 ו-§7 שורה 44 */
  sent: "שויך",
  viewed: "נצפה",
  done: "טופל",
  removed: "הוסר",
} as const;

/** סטטוס פנייה נגזר — §3.5, שורות 154–160 */
export const TICKET_STATUS = {
  closed: "סגור",
  draft: "טיוטה",
  awaitingApproval: "טופל — ממתין לאישור סופי",
  partial: "בטיפול חלקי",
  viewed: "נצפה",
  fresh: "חדש",
  /** שורה 164 — תג על הכרטיס, לא סטטוס */
  reopenedBadge: "נפתחה מחדש",
} as const;

/** מסך 2 — הודעות למשתמש, שורות 226–228 */
export const TICKET_SCREEN = {
  closedNotice: "הפנייה נסגרה.",
  reopenedNotice: "הפנייה נפתחה מחדש. הנמענים קיבלו התראה.",
  linkResent: (name: string) => `הקישור נשלח שוב ל${name}.`,
  /** שורות 218–223 — שמות הפעולות */
  send: "שלח",
  markHandler: "סמן: אני מטפל",
  editRecipients: "ערוך נמענים",
  resendEmail: "שלח שוב במייל",
  sendWhatsApp: "שלח בוואטסאפ",
  newLink: "צור קישור חדש",
  close: "סגור פנייה",
  reopen: "פתח מחדש",
} as const;

/**
 * מסך 2, שורה 216 — ארבעת אירועי המערכת.
 *
 * האפיון מציג אותם כדוגמאות (`שויך לרונית`, `הוסר מיכאל`,
 * `נצפה על ידי יוסי`, `נפתחה מחדש`) ולא כמחרוזות מחייבות תחת "הודעות
 * למשתמש". המימוש בחר בסדר הפוך, נושא-ראשון ("רונית שויך לפנייה"), שהוא
 * עברית טבעית יותר. לכן ההתאמה כאן היא **סמנטית ולא מילולית**: השם והפועל
 * חייבים להופיע, הסדר לא. הסטייה מהדוגמאות מדווחת ב-conformance-report.
 */
export const THREAD_EVENTS = {
  assigned: (name: string) => new RegExp(`(שויך.*${name}|${name}.*שויך)`),
  removed: (name: string) => new RegExp(`(הוסר.*${name}|${name}.*הוסר)`),
  viewed: (name: string) => new RegExp(`(צפה.*${name}|${name}.*צפה)`),
  reopened: /מחדש/,
} as const;

/** מסך 3 — הודעות למשתמש, שורות 239–241 */
export const RECIPIENTS_SCREEN = {
  confirmAdd:
    "לנמען שנוסף תיחשף כל היסטוריית השיחה בפנייה, כולל תמונות והקלטות. להוסיף?",
  confirmRemove: (name: string) =>
    `הפנייה תיעלם מהרשימה של ${name} והוא לא יוכל להגיב יותר. התגובות שכתב יישארו. להסיר?`,
  missingContact: "חסר טלפון או מייל — בלעדיהם לא ניתן לשלוח לו את הפנייה.",
} as const;

/**
 * שלוש יכולות המדיה — מסך 2 אזור ד׳ (שורה 247): "טקסט, צירוף תמונה,
 * הקלטת קול", ומסך 4 §7 #21 (שורה 496): "צלם" נשאר **נפרד** מ"צרף קובץ".
 *
 * האפיון מתאר כאן יכולות ולא נוסחי כפתור, ולכן המחרוזות הן הנוסח שנבחר
 * במוצר. הן נבדקות בשמן הנגיש: מרגע שהכפתורים בקומפוזר עברו לאייקונים
 * (DESIGN.md § אייקונים) השם הנגיש מגיע מ-`aria-label`, וזהותו למחרוזת
 * שהייתה התווית הגלויה היא בדיוק מה שהטענות האלה שומרות.
 */
export const MEDIA = {
  attach: "צרף קובץ",
  camera: "צלם",
  record: "הקלט",
  /**
   * ה-"+" שפותח את גיליון הצירוף בקומפוזר (§7 שורה 34).
   *
   * נוסח מוצר ולא נוסח אפיון — האפיון מונה **יכולות** ולא כפתורים (ראו
   * ההערה מעל). "צירוף" ולא "צרף": `getByRole(name)` הוא התאמת תת-מחרוזת,
   * ו"צרף" היה תופס גם את "צרף קובץ" שבתוך הגיליון.
   */
  attachMenu: "צירוף",
} as const;

/** מסך 4 — פעולות והודעות, שורות 259–264 */
export const CREATE_SCREEN = {
  submit: "שלח לנמענים",
  saveDraft: "שמור כטיוטה",
  sentTo: (names: string) => `הפנייה נשלחה ל${names}.`,
  savedAsDraft: "נשמר כטיוטה. לא נשלח לאיש.",
} as const;

/** מסך 5 — פעולות והודעות, שורות 279–284 */
export const BATCH_SCREEN = {
  addRow: "הוסף שורה",
  dispatchAll: "שגר הכל",
  saveAllAsDraft: "שמור הכל כטיוטה",
  created: (tickets: number, professionals: number) =>
    `נוצרו ${tickets} פניות ושויכו ל-${professionals} אנשי מקצוע.`,
  missingRecipients: (rows: number) => `${rows} שורות חסרות נמען. הן נשמרו כטיוטה.`,
} as const;

/** מסך 6 — הודעה למשתמש, שורה 296 */
export const TAG_SCREEN = {
  granted: (names: string) =>
    `התגית נפתחה ל${names}. הם יראו את הצ׳אט הקבוצתי בלבד, לא את הפניות.`,
} as const;

/** מסך 7 — טיוטה, שורות 305–306 */
export const DRAFT_SCREEN = {
  /** מסך 7 — **רק כשבאמת חסרים פרטים** (עדכון 1.3) */
  banner: "טיוטה — חסרים פרטים. לא נשלחה לאיש.",
  /** מסך 7 — בכל טיוטה שלא חסר בה דבר, ממייל או מ"שמור כטיוטה" (EM-S7-06) */
  readyBanner: "טיוטה — לא נשלחה לאיש.",
  /** מסך 7, טיוטה ממייל בסתירה (EM-S7-04) */
  conflictBanner: (fields: number) =>
    fields === 1
      ? "יש סתירה בין המייל למערכת ב-1 שדה. לא ניתן לשגר עד שתוכרע."
      : `יש סתירה בין המייל למערכת ב-${fields} שדות. לא ניתן לשגר עד שתוכרע.`,
  submit: "שגר",
  delete: "מחק טיוטה",
} as const;

/** מסך 8 — פורטל הקבלן, שורות 319–329 */
export const PORTAL = {
  markDone: "סיימתי — טופל",
  doneNotice: (opener: string) => `תודה. הפנייה הועברה לאישור ${opener}.`,
  /**
   * שורה 329 — משפט אחד. המימוש מפצל אותו לכותרת ולפסקה ומשמיט את הנקודה
   * הראשונה (`expired-link.tsx:19-20`), ולכן חיפוש המשפט השלם ככתבו אינו
   * מוצא דבר. שני החלקים נשמרים כאן בנפרד, והפיצול מדווח כפער ניסוח.
   */
  expired: "הקישור אינו בתוקף. פנה למנהל העבודה שלך.",
  expiredTitle: "הקישור אינו בתוקף",
  expiredHelp: "פנה למנהל העבודה שלך.",
} as const;

/** §5.ו — מקרי קצה, שורות 386, 389 */
export const EDGE_CASES = {
  closedTicketBlocked: "הפנייה נסגרה. פנה למנהל העבודה.",
  transcriptionFailed: "התמלול נכשל",
} as const;

/**
 * §3.3 — רשימת החדרים הקבועה, שורה 132.
 * **11 ערכים בדיוק, בעברית.** חצרות ושטחי חוץ אינם בתחולה (שורה 134).
 */
export const ROOMS_HE = [
  "סלון",
  "מטבח",
  "חדר שינה",
  "חדר רחצה",
  "שירותים",
  "מרפסת",
  "ממ״ד",
  "חדר מדרגות",
  "חניה",
  "לובי",
  "שטח משותף",
] as const;

/** §3.2 שדה 13 — ערוץ המקור (`ממייל` נוסף בעדכון 1.3) */
export const CHANNELS_HE = ["אני", "מההנהלה", "מוואטסאפ", "ממייל"] as const;

/** §3.6 — תשעת המסננים, שורה 170 */
export const SEARCH_FILTERS = [
  "אתר",
  "בניין",
  "דירה",
  "תחום",
  "נמען",
  "תגית",
  "סטטוס",
  "טווח תאריכים",
  "הפניתי / קיבלתי",
] as const;

/** §5.ג — סף ההסלמה, שורה 363 */
export const STALE_DAYS = 7;

/**
 * המיילים היוצאים לשולח מייל פנייה — "המיילים היוצאים לשולח (עדכון 1.3)"
 * בסוף §4, שורות 526–580 באפיון 1.3.1, ו-§7 שורות 71–72 (שורות 824–825).
 *
 * **ההדגשות (`**…**`) הוסרו.** באפיון הן סימון Markdown של כותרות החלקים,
 * לא חלק מהנוסח; במייל הטקסטואלי אין להן ייצוג, ובמייל ה-HTML הן `<strong>`
 * שמפצל את הטקסט לכמה צמתים. מה שנבדק הוא המילים והסימנים.
 *
 * **הדוגמאות של המייל הכללי הן שורות שלמות** (שורות 542–550), עם הערכים
 * שהאפיון בחר ("מיזוג", "יוסי", "במייל 14"). הבדיקה מזינה בדיוק את אותם
 * ערכים ומצפה לשורה כלשונה — כך נבדק גם החיבור בין החלקים (רווחים, נקודות,
 * המפריד `—` והחץ `←`) ולא רק המילים הקבועות.
 *
 * מקומות שבהם האפיון כותב `[קישור]`, `[מספר]` או `[שם]` הם פונקציות. קריאה
 * להן עם הטקסט שבסוגריים מחזירה את השורה כפי שהיא כתובה באפיון, וכך
 * `email-templates.test.ts` מוודא שהקובץ הזה עצמו הועתק נכון.
 */
export const EMAIL_REPLY_SPEC = {
  /** שורה 534 */
  greeting: (name: string) => `שלום ${name},`,
  /** שורה 536 — המשפט השני מודגש באפיון */
  received: "המייל שלך התקבל ונשמר כטיוטה במערכת בקרת פניות. הטיוטה עוד לא נשלחה לאיש.",
  /** שורה 538 — "מופיע תמיד" (שורה 532) */
  currentHeading: "מה יש בטיוטה עכשיו:",
  /** שורה 539 — כל השדות, בסדר הזה */
  summaryLine: (v: {
    site: string;
    building: string;
    apartment: string;
    room: string;
    domain: string;
    recipients: string;
  }) =>
    `אתר: ${v.site} · בניין: ${v.building} · דירה: ${v.apartment} · חדר: ${v.room} · תחום: ${v.domain} · נמענים: ${v.recipients}`,
  /** שורה 540 */
  descriptionLine: (value: string) => `תיאור: ${value}`,
  /** שורה 542 — דוגמה שלמה; מופיע רק במייל שעונה על תשובה (שורה 554) */
  updatedExample: "עודכן מהתשובה שלך: חדר (מטבח ← חדר רחצה)",
  updatedHeading: "עודכן מהתשובה שלך:",
  /** שורה 544 */
  missingExample: "חסר: נמענים (מי יטפל בתקלה)",
  missingHeading: "חסר:",
  /** שורה 546 */
  notFoundExample: 'לא נמצא ברשימה: תחום: כתבת "מיזוג". התחומים הקיימים: חשמל, אינסטלציה, אלומיניום.',
  notFoundHeading: "לא נמצא ברשימה:",
  /** שורה 548 */
  ambiguousExample: 'נמצאו כמה התאמות: נמענים: כתבת "יוסי" — יוסי כהן, יוסי לוי. כתבו בתשובה את השם המלא.',
  ambiguousHeading: "נמצאו כמה התאמות:",
  /** שורה 550 */
  conflictExample: "סותר את מה שנקבע במערכת: דירה: במייל 14, במערכת 12. ההכרעה תיעשה במערכת.",
  conflictHeading: "סותר את מה שנקבע במערכת:",
  /** שורה 552 */
  howTo: (link: string) =>
    `איך משלימים: משיבים למייל הזה וכותבים רק מה שחסר או צריך תיקון (למשל: "דירה 12, הנמען יוסי כהן"), או משלימים במערכת: ${link}`,
  howToHeading: "איך משלימים:",
  /** שורה 564 — "כשלא חסר דבר ואין סתירה", במקום "איך משלימים" */
  ready: (link: string) =>
    `כל הפרטים זוהו. כדי לשלוח את הפנייה לנמענים: ${link}. אם משהו לא נכון, השב עם התיקון.`,
  /** שורה 568 */
  afterDispatch: (seq: number | string, link: string) =>
    `פנייה #${seq} כבר נשלחה לנמענים, ולכן התשובה הזו לא שינתה בה דבר. עדכונים לפנייה נכתבים במערכת: ${link}`,
  /** שורה 570 — הגרש סביב 'תקלה' הוא ASCII, כמו באפיון */
  afterDeletion:
    "הטיוטה נמחקה במערכת, ולכן התשובה הזו לא נקלטה. לפתיחת פנייה חדשה שלחו מייל חדש שהכותרת שלו כוללת 'תקלה'.",
  /** שורה 572 */
  extractionUnavailable: (link: string) =>
    `המייל נשמר כטיוטה, אבל הפרטים לא חולצו ממנו אוטומטית כי השירות אינו זמין כרגע. תוכן המייל נשמר כתיאור. את שאר הפרטים יש להשלים במערכת: ${link}`,
  /** שורה 574 */
  extractionUnavailableReply: (link: string) =>
    `התשובה שלך נשמרה בהתכתבות של הטיוטה, אבל לא עובדה אוטומטית כי השירות אינו זמין כרגע. את השינויים יש להזין במערכת: ${link}`,
  /** שורה 576 */
  notPermitted: (senderName: string) =>
    `אין לך הרשאה לעדכן את הטיוטה הזו, ולכן התשובה שלך לא נקלטה. אפשר לפנות ל${senderName}.`,
  /** שורה 578 */
  noSite: "לא ניתן לפתוח פנייה מהמייל הזה, כי אינך משויך כרגע לאתר. פנה למנהל המערכת.",
  /** §7 שורה 71 (שורה 824) — ערך ריק ב"מה יש בטיוטה עכשיו" */
  emptyValue: "—",
  /** §7 שורה 72 (שורה 825) — באפיון "…" במקום הרשימה */
  missingSiteWithOptions: (sites: string) => `חסר: אתר. האתרים הקיימים: ${sites}`,
} as const;
