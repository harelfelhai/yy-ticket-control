# דוח פערים — אימות התאמה מלא, 3.8.2026

> **תוצר של סבב אימות מול שני מקורות האמת של המערכת. לא בוצע שום תיקון.**
>
> | מקור אמת | מיקום |
> |---|---|
> | האפיון הפונקציונלי, גרסה 0.2 (456 שורות) | `docs/specs/ticket-control-pre-plan.md` |
> | תוכנית המימוש הסופית, 22.7.2026 (22,826 תווים) | **לא הייתה על הדיסק.** שוחזרה מקריאת `ExitPlanMode` בתמליל סשן הבנייה |
> | מטריצת העקיבות (388 דרישות) | `docs/specs/conformance-matrix.md` |
>
> **מתודולוגיה:** כל assertion בחבילת ההתאמה משווה מול מחרוזת שהועתקה ידנית
> מטקסט האפיון (`conformance/fixtures/spec-text.ts`), ולעולם לא מול import
> מ-`src/lib/he.ts`. בדיקה שמייבאת מ-`he.ts` מוכיחה שהקוד עקבי עם עצמו
> ואינה יכולה לגלות שהוא סוטה מהאפיון — וזה בדיוק דפוס הכשל שכבר תועד
> בפרויקט הזה (פערים 1, 9, 10, 14 סומנו ✅ בזמן שהיו פתוחים).

<!-- SUMMARY -->

---

## 1. פערי מימוש מול האפיון

### GAP-A1 · החלפת מטפל אינה אפשרית — §5.ד, הכרעת מימוש 0.2

| | |
|---|---|
| **הדרישה** | *"החלפת מטפל נעשית **מפורשות דרך הכפתור**."* (שורה 373) |
| **מה נמצא** | `ticket-actions.tsx:101` מרנדר את הכפתור "סמן: אני מטפל" בתנאי `canSetHandler && !hasHandler && !isClosed`. ברגע שנקבע מטפל ראשון — בין אם ידנית ובין אם אוטומטית מתגובה — הכפתור נעלם, ואין בממשק שום דרך אחרת להחליף מטפל. |
| **חומרה** | **גבוהה.** זהו בדיוק המצב שהסייג של 0.2 נכתב כדי לפתור: "כך מנהל שני שמגיב לפנייה שכבר יש לה מטפל אינו 'גונב' את הסימון". הסייג האוטומטי מומש; המנגנון הידני שאמור להחליף אותו — לא. |
| **ראיה** | `conformance/specs/br-rules.spec.ts` → `BR-22/BR-23`, מסומן `test.fail()` |
| **הערה** | הכלל המשלים BR-24 ("הסימון אינו נועל את הפנייה") **מתקיים**: מנהל אחר עדיין קורא, מגיב ועורך נמענים. הפגיעה היא בהחלפת הסימון בלבד. |

### GAP-A2 · נמען פנימי אינו רואה פניות מאתר אחר — §5.ז, §2.2 שלב 3

| | |
|---|---|
| **הדרישה** | *"נמען לטיפול: אך ורק פניות ששויכו אליו, **מכל האתרים**"* (שורה 402), ו*"נמען פנימי רואה את הפנייה **בלוח הרגיל שלו**"* (שורה 52). |
| **מה נמצא** | שני מנגנונים בלתי תלויים חוסמים את התרחיש: (1) `tickets/new/page.tsx:62` מציע כנמענים פנימיים רק משתמשים ש-`siteId` שלהם שווה לאתר הפנייה או `null`; (2) `permissions.ts:64` — `canViewTicket` עבור `SITE_MANAGER` בודק **אך ורק** `viewer.siteId === ticket.siteId` ואינו מביט בשיוכים כלל. |
| **תוצאה** | מנהל עבודה של אתר א׳ אינו ניתן לשיוך כנמען בפנייה של אתר ב׳, וגם אילו שויך — לא היה רואה אותה. רק `ADMIN` ו-`OWNER` (שאינם משויכי-אתר) ניתנים לשיוך חוצה-אתרים. |
| **חומרה** | **בינונית.** תרחיש "עובד לעובד" (§2.1) עדיין עובד בתוך אתר. הפער הוא בהיקף חוצה-האתרים שהאפיון מדגיש. |
| **ראיה** | `conformance/specs/br-permissions.spec.ts` → `A4-03` · `tests/conformance/db/rules.test.ts` → `A3-01` |

### GAP-A3 · אינדקסי החיפוש נמחקו ולא שוחזרו — תוכנית M3 §4

| | |
|---|---|
| **הדרישה** | תוכנית המימוש, M3 סעיף 4: *"חיפוש (מסך 9) כולל תמלולים וטקסט מחולץ + **אינדקס pg_trgm**"*. `schema.prisma` מתעד אותם כקיימים. |
| **מה נמצא** | `prisma/migrations/20260722180000_search_trigram_indexes` יוצרת ארבעה אינדקסי GIN. `prisma/migrations/20260724054950_add_rate_limit/migration.sql:2-11` **מוחקת את כל הארבעה**, ואף מיגרציה מאוחרת אינה יוצרת אותם מחדש. |
| **אימות בפועל** | נבדק בשאילתה ישירה: **מסד מקומי — 0 אינדקסי trgm/GIN. מסד הפרודקשן ב-Railway — 0 אינדקסי trgm/GIN.** התוסף `pg_trgm` מותקן בשני המסדים, האינדקסים אינם. |
| **חומרה** | **בינונית.** החיפוש נכון פונקציונלית (`ILIKE`), אך סורק סדרתית ארבע טבלאות. בנפח הנוכחי (0 פניות בפרודקשן) אינו מורגש; ב"תרחיש המתפרץ" של בדק בית הוא הפער שיתגלה ראשון. |
| **ראיה** | `tests/conformance/source/scope-boundaries.test.ts` → `DM-Q04` — הבדיקה מקבעת את המצב ותיהפך לאדומה כשמיגרציה תשחזר אותם |

### GAP-A4 · תוכן מסך 404 באנגלית — §4 שורה 176

| | |
|---|---|
| **הדרישה** | *"שפת הממשק: עברית, כיווניות מימין לשמאל"* — ללא חריג. |
| **מה נמצא** | אין `not-found.tsx`, אין `error.tsx` ואין `global-not-found` בשום מקום בעץ `src/app`. `notFound()` נקרא מ-`/tickets/[id]` ומ-`/tags/[id]`, כלומר כל חסימת הרשאה חוצת-אתרים מגיעה למסך ברירת המחדל של Next. |
| **מה שכן תקין** | **המעטפת עברית ו-RTL.** `app/layout.tsx` עוטף גם את מסך ברירת המחדל, ולכן `lang="he"` ו-`dir="rtl"` מתקיימים. הבדיקה שהנחתי שתיכשל — **עברה**, וההנחה המקורית תוקנה. |
| **מה שנותר פער** | **הטקסט**: `This page could not be found` — אנגלית, בלי ניווט חזרה ובלי הסבר. |
| **חומרה** | **נמוכה־בינונית.** מסלול שכיח, אך המשתמש עדיין מזהה שהוא בתוך המערכת. |
| **ראיה** | `conformance/specs/rtl-mobile.spec.ts` → `S0-04` (מעטפת, עובר) ו-`S0-04 תוכן` (מסומן `test.fail()`) |

### GAP-A8 · אזורי מגע מתחת ל-44px שאף אוכף אינו רואה — `DESIGN.md § Touch`

| | |
|---|---|
| **הדרישה** | סף מגע 44px (WCAG 2.5.5 / Apple), מעוגן ב-`docs/DESIGN.md` ונאכף ב-`tests/unit/layout-guards.test.ts`. |
| **מה נמצא (מדידה בפועל על Pixel 5)** | `"×"` — כפתור הסרת צ׳יפ הנמען (`recipient-picker.tsx:66`): **16px**. `"← חזרה לרשימה"` בפורטל הקבלן: **20px**. `"ערוך"` של שם הדייר: **36px** — זה חריג מסונקצן (קישור בתוך משפט, כבר ברשימת החריגים של `primitives.test.ts`). |
| **למה אף אוכף לא תפס** | `layout-guards.test.ts` סורק **מחלקות `min-h-*` בקוד המקור**, ולכן אלמנט שאינו מצהיר `min-h` כלל אינו נראה לו. `e2e/mobile-qa.spec.ts` מודד תיבות אמיתיות — אך על ארבעה מסכים בלבד, ואף אחד מהם אינו מסך היצירה או הפורטל. שני האוכפים משלימים זה את זה ומשאירים ביניהם חור. |
| **חומרה** | **בינונית** לפורטל: קהל היעד הוא קבלן בשטח, על טלפון, אולי בכפפות — וזה בדיוק הנימוק שהאפיון נותן לסף (§4 שורה 177). |
| **ראיה** | `conformance/specs/rtl-mobile.spec.ts` → `S0-03` (מסומן `test.fail()`) והסריקה הרוחבית ב-"מסכי הפורטל" |

### GAP-A5 · נוסח "חסר טלפון או מייל" אינו זה שבאפיון — §4 מסך 3, שורה 241

| | |
|---|---|
| **הדרישה** | `"חסר טלפון או מייל — בלעדיהם לא ניתן לשלוח לו את הפנייה."` |
| **מה נמצא** | המחרוזת אינה קיימת ב-`he.ts`. במקומה שני ניסוחים אחרים: `directory.contactRequiredHint` = `"צריך טלפון או מייל — בלעדיהם אי אפשר לשלוח אליו את הפנייה"` (רמז מקדים, לא הודעת שגיאה), ו-`delivery` = `"לא ניתן לשגר: לנמען אין טלפון ואין מייל"` (חיווי שליחה). |
| **חומרה** | **נמוכה.** ההתנהגות הנדרשת מתקיימת — היצירה נחסמת ומוצגת סיבה. הפער הוא בנוסח. |
| **ראיה** | `conformance/specs/prohibitions.spec.ts` → `S3-08`, מסומן `test.fail()`; ההתנהגות עצמה נבדקת ועוברת ב-`PROH-20/BR-39` |

### GAP-A6 · נוסח "הקישור אינו בתוקף" מפוצל — §4 מסך 8, שורה 329

| | |
|---|---|
| **הדרישה** | `"הקישור אינו בתוקף. פנה למנהל העבודה שלך."` — משפט אחד. |
| **מה נמצא** | `expired-link.tsx:19-20` מפצל לכותרת (`"הקישור אינו בתוקף"`, **בלי הנקודה**) ולפסקה (`"פנה למנהל העבודה שלך."`). |
| **חומרה** | **נמוכה.** ההתנהגות תקינה לחלוטין והפיצול משפר קריאוּת. נרשם כדי שהמסמך והקוד לא ייחשבו זהים כשאינם. |

### GAP-A7 · מסך 15 (תגיות) אינו חסום למנהל המערכת בלבד — §4 שורה 345

| | |
|---|---|
| **הדרישה** | *"מסכים 11–15: **מנהל מערכת ראשי בלבד**"*, כאשר מסך 15 הוא ניהול התגיות. |
| **מה נמצא** | ארבעת המסכים שתחת `/admin` חסומים כנדרש (`admin/layout.tsx:13`). מסך התגיות יושב ב-`/tags` — מחוץ ל-`/admin` — ונגיש למנהל עבודה ולבעלים. רק פעולת "שנה שם" מוגבלת ל-ADMIN (`tags/page.tsx:23`). |
| **הערכה** | ייתכן שזו הכרעה נכונה: פתיחת תגית לקבלן מוגדרת ב-§5.ה כפעולת "המנהל", ומנהל עבודה זקוק לה. אך היא **סותרת את הניסוח המפורש** של §4 שורה 345, ולא תועדה כהכרעה. |
| **חומרה** | **נמוכה־בינונית** — טעונה הכרעה שלך, לא תיקון מכני. |

### GAP-A9 · ניסוח אירועי המערכת שונה מדוגמאות האפיון — §4 מסך 2, שורה 216

| | |
|---|---|
| **הדוגמאות באפיון** | `שויך לרונית` · `הוסר מיכאל` · `נצפה על ידי יוסי` · `נפתחה מחדש` |
| **מה שהמערכת מציגה** | `רונית שויך לפנייה` · `מיכאל הוסר מהפנייה` · `יוסי צפה בפנייה` · `X פתח את הפנייה מחדש` |
| **הערכה** | סדר המילים הפוך (נושא ראשון), והניסוח של המימוש טבעי יותר בעברית. האפיון מציג את הארבעה כ**דוגמאות** בתוך משפט, ולא תחת הכותרת "הודעות למשתמש" שבה הוא מקפיד על ציטוט מדויק — ולכן זו ככל הנראה חירות מכוונת ולגיטימית. |
| **חומרה** | **נמוכה.** נרשם כדי שהמסמך והקוד לא ייחשבו זהים כשאינם. הבדיקה מאמתת סמנטיקה (שם + פועל) ולא סדר. |



---

## 2. פערי מימוש מול תוכנית המימוש הסופית

### 2.1 דלתות מאושרות (הוכרעו בשיחה, מתועדות)

| # | התוכנית | בפועל | ההכרעה |
|---|---|---|---|
| 1 | **PWA עם service worker**, cache ל-shell, התקנה לטלפון (M6.1) | `manifest.webmanifest` בלבד; אין service worker | **החלטתך ב-24.7:** *"לגבי M6 - PWA זה בשביל שהמערכת תהיה apk? כי אם כן - אין צורך. אני רוצה שזה יישאר ממשק ווב"* |
| 2 | גיבוי לילי **לגוגל דרייב העסקי** (M6.3, Gate G6) | bucket שני ב-R2 | Gate G6 הוכרע: חשבון Google One ולא Workspace, ולכן ה-fallback שהתוכנית עצמה הגדירה |
| 3 | ריפו מרוחק (Gate G7) | ריפו פרטי ב-GitHub | אושר |
| 4 | `Next.js 15` | `Next.js 16.2.11` | הגרסה היציבה בזמן הבנייה; `AGENTS.md` מתעד את השינויים השוברים |

### 2.2 דלתות שלא נרשמו כהכרעה

| # | התוכנית | בפועל | הערכה |
|---|---|---|---|
| 5 | **שכבת API כ-route handlers**: 14 מסלולים תחת `src/app/api/` (סעיף 4 בתוכנית, טבלה מלאה) | **3 route handlers בלבד** (`/api/health`, `/api/media/[id]`, `/api/media/upload/[...key]`); כל השאר Server Actions ב-9 קבצים | **הדלתא הארכיטקטונית הגדולה בפרויקט.** הבחירה נכונה ל-Next 16 ומייתרת שכבת fetch שלמה, אך אינה כתובה בשום מקום כהכרעה — ומי שיקרא את התוכנית יחפש API שאינו קיים |
| 6 | **שתי סביבות Railway**, `dev` ו-`production`, לכל אחת DB (סעיף 1) | סביבה אחת (`production`); פיתוח מקומי מול `embedded-postgres` על 5433 | שיפור מעשי (אין תלות רשת בפיתוח), אך פריסה נבדקת רק מול הסביבה החיה |
| 7 | `argon2` | `@node-rs/argon2` | מנומק בקוד (בינאריים מוכנים ל-Windows) |
| 8 | סוגי ג׳ובים `OCR` · `SEND_EMAIL` | `EXTRACT` · `SEND_NOTIFICATION` | שינוי שמות; `SEND_NOTIFICATION` מדויק יותר |
| 9 | `MediaFile.r2Key` | `MediaFile.storageKey` + `uploaded` | שיפור: הפשטת אחסון שמאפשרת דרייבר מקומי |
| 10 | `Message.eventType` — 8 ערכים | 11 ערכים (נוספו `FIELDS_EDITED`, `TAG_GRANTED`, `TAG_REVOKED`) | הרחבה עקבית |

<!-- PLAN-VERIFY -->

---

## 3. סתירות פנימיות באפיון עצמו — **אינן באג בקוד**

| # | הסתירה | שורות |
|---|---|---|
| GAP-01 | §2.2 שלב 7 כותב **"דורש ממני"**; מסך 1 ו-§5.ג כותבים **"דורש ממך"**. המימוש בחר ב"דורש ממך" | 59 מול 185, 363 |
| GAP-02 | הכותרת אומרת גרסה **0.2**, שורת הסיום אומרת **0.1** | 3 מול 455 |
| GAP-03 | §2.2 קובע שנמען פנימי "רואה את הפנייה בלוח הרגיל שלו", אך **אין מסך מוגדר לנמען פנימי** — קהל היעד של מסכים 1 ו-2 אינו כולל אותו | 52, 181, 210 |
| GAP-04 | `Channel` בעל **3** ערכים מול **5** נקודות כניסה (§2.1). אין מיפוי מוגדר ל"עובד לעובד", "בדק בית" ו"פנייה לעצמי" | 40–46 מול 118 |
| GAP-05 | §3.5 **אינו מכסה מצב שכל השיוכים הוסרו**. שבעת הכללים נופלים לכלל 7 ומחזירים "חדש" — פנייה בלי אף נמען פעיל מוצגת כאילו זה עתה נשלחה | 152–160 |
| GAP-06 | סייג 0.2 ל-§5.ד ("רק אם עדיין אין מטפל") **לא עודכן** ב-§2.4 ובמסך 2, ששניהם עדיין מתארים סימון ללא תנאי | 74, 218 מול 373 |
| GAP-07 | §7 שאלה 5 (תפוגת הקישור) רשומה **פתוחה**, בעוד מסך 2 כבר הכריע שהקישור יציב | 432 מול 221 |
| GAP-08 | §5.ו אינו מכסה לאן עובר נמען בסטטוס "שאלה" כשהפנייה נסגרת | 145 |
| GAP-09 | בעלים פותח פניות בכל אתר, אך **אינו בקהל היעד של מסך 3** (עריכת נמענים) — לא ברור כיצד ישייך | 245 מול 233 |
| GAP-10 | לא מוגדר **למי** מוצגת טיוטה תחת "דורש ממך": לפותח בלבד או לכל מנהלי האתר | 185 |
| GAP-11 | פנייה **מלאת שדות** שנשמרה במפורש כטיוטה אינה נתפסת ע"י אף אחד מ-7 כללי §3.5 (כלל 2 מותנה ב"חסרים שדות חובה") | 156 מול 260, 280 |
| GAP-12 | **אין הגדרה של "התראה"** — ערוץ, פורמט ותזמון אינם מוגדרים בשום מקום | 52, 69 |
| GAP-13 | §7 שאלה 4 (האם שאלת נמען חוזרת גם לשאר הנמענים) פתוחה ו**חוסמת** — משפיעה ישירות על ההתראות. המימוש בחר בברירת המחדל של Gate G3: לפותח בלבד | 431 |

---

## 4. ממצאי איכות שאינם פערי אפיון

| # | הממצא | ראיה |
|---|---|---|
| Q1 | **20 מחרוזות ב-`he.ts` שאינן מופנות מאף מקום** — אומת בגריפ: `ticket.sentTo`, `ticket.savedAsDraft`, `ticket.editRecipients`, `ticket.addRecipients`, `notices.transcriptionFailed`, `notices.linkExpired`, `overview.forbidden`, `board.batchEntry`, `board.count`, `common.loading`, `common.close`, `common.back`, `portal.emptyArchive`, `portal.groupChatsEmpty`, `media.recording`, `media.download`, `batch.rowRecipient`, `admin.newSite`, `admin.tags`, `admin.ownerAdminNoSite` — **כולן 0 מופעים** |
| Q2 | שתי המחרוזות הראשונות ב-Q1 הן **נוסחי האפיון S4-14 ו-S4-15** (`"הפנייה נשלחה ל[שמות]."` · `"נשמר כטיוטה. לא נשלח לאיש."`). הן קיימות בקוד ואינן מוצגות למשתמש בשום מסלול |
| Q3 | **כפילות ניסוח לאותו מצב**: `notices.transcriptionFailed` מול `ai.transcriptionFailed`, `notices.linkExpired` מול `portal.expired` — סיכון היסט |
| Q4 | **אף `EmptyState` אינו מעביר `action`** — 7 מופעים, 0 עם CTA, בעוד `empty-state.tsx:11` מתעד את הדרישה בעצמו |
| Q5 | דף ניהול שנטען בידי מי שאינו ADMIN מפעיל `assertAdmin` **וזורק** לפני שההפניה של ה-layout מסתיימת. נצפו שלוש שגיאות שרת בלוג (`אזור הניהול זמין למנהל המערכת בלבד`) בכל ניסיון גישה. ההפניה גוברת והמשתמש אינו רואה שגיאה, אך בפרודקשן זה מגיע ל-Sentry |

---

## 5. מה לא הורץ, ולמה

| הנושא | הסיבה | מה נדרש כדי לסגור |
|---|---|---|
| **תמלול עברית אמיתי** (WF-05, S4-04, TC-03) | `OPENAI_API_KEY` אינו מוגדר בסביבה. בלעדיו הג׳וב מסמן `SKIPPED` — התנהגות מתוכננת ("הקלטה בלי תמלול היא עדיין הקלטה"), אך היא **אינה** אימות של הדרישה | הקלטה בעברית מהטלפון מול מפתח אמיתי, ואימות שהתמלול ממלא את שדה התיאור הריק |
| **חילוץ טקסט אמיתי מ-PDF/תמונה** (WF-05, S5-03) | `ANTHROPIC_API_KEY` אינו מוגדר; אותו מסלול `SKIPPED` | צילום דוח בדק בית אמיתי, ואימות שהטקסט נכנס למנוע החיפוש |
| **מקרה הקצה BR-35 בכשל אמיתי** | הכשל הודמה בקביעת `aiStatus = FAILED` ישירות במסד. זו הבאת מצב תקינה, אך אינה מוכיחה שהג׳וב מגיע ל-`FAILED` בעצמו בכשל ספק אמיתי | ניתוק/שגיאת ספק מכוונת מול מפתח אמיתי |
| **הסלמת 7 הימים בזמן אמת** (BR-10) | נבדקה בדחיפת `lastActivityAt` אחורה והרצת הג׳וב **האמיתי** (`runDailyEscalation`). מה שלא נבדק הוא ש**התזמון היומי ל-06:00 יורה בפועל** | תצפית על `Heartbeat` ועל Sentry Cron לאורך יממה בפרודקשן |
| **הגיבוי הלילי ב-03:00** (M6.3) | תלוי זמן ותלוי `R2_BACKUP_BUCKET` בפרודקשן | אותה תצפית — ה-heartbeat מתעדכן או ה-watchdog מתריע |
| **מייל אמיתי דרך Resend** (V02-09, WF-03) | בסביבה המקומית פועל `consoleTransport`. הנבדק היה שהפעולה מתבצעת ושהחיווי מתעדכן, לא שהמייל הגיע לתיבה | סבב `prod-qa` מול Resend מוגדר, ובדיקת תיבה אמיתית |
| **‏QA על מכשיר פיזי** (M6.5, תוכנית) | Playwright מדמה Pixel 5 ו-Desktop Chrome. `e2e/mobile-qa.spec.ts` מריץ גם WebKit/iPhone 13 — קירוב, לא מכשיר | פתיחת קישור קבלן מטלפון אמיתי בשמש, עם כפפות |
| **‏`wa.me` מטלפון אמיתי** (V02-10) | נבדק שה-`href` נכון ומכיל את ההודעה והקישור. פתיחת וואטסאפ עצמה אינה ניתנת לאוטומציה | לחיצה מטלפון של מנהל אמיתי |

